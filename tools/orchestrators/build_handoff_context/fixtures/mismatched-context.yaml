name: orchestrator-handoff-mismatched-context
lane: deterministic
target:
  kind: tool
  ref: orchestrators.build_handoff_context
inputs:
  platform: zapier
  event_id: evt_zapier_demo_001
  handoff_scope: orchestrator.zapier.workflow.invoke
  handoff_audience: zapier:zap:runx-governed-effect
  execution_context:
    caller: runx-cli
    event_id: evt_other
    workflow_ref: zapier-demo
  payload:
    hello: zap
expect:
  status: failure
  output:
    subset:
      status: needs_input
      reason_code: invalid_handoff_context
