name: orchestrator-n8n-handoff-external-contract
lane: external
target:
  kind: tool
  ref: orchestrators.n8n_handoff
inputs:
  webhook_host: n8n.example.com
  workflow_slug: runx-governed-effect
  event_id: evt_n8n_demo_001
  handoff_scope: orchestrator.n8n.workflow.invoke
  handoff_audience: n8n:workflow:runx-governed-effect
  execution_context:
    caller: runx-cli
    workflow_ref: self-hosted-n8n-demo
  payload:
    hello: workflow
expect:
  status: success
metadata:
  purpose: contract-only fixture for the live HTTP handoff manifest
