name: orchestrator-zapier-handoff-external-contract
lane: external
target:
  kind: tool
  ref: orchestrators.zapier_handoff
inputs:
  zapier_account_id: "123456"
  zapier_hook_id: abcdef
  event_id: evt_zapier_demo_001
  handoff_scope: orchestrator.zapier.workflow.invoke
  handoff_audience: zapier:zap:runx-governed-effect
  execution_context:
    caller: runx-cli
    workflow_ref: zapier-demo
  payload:
    hello: zap
expect:
  status: success
metadata:
  purpose: contract-only fixture for the live HTTP handoff manifest
