name: control-capture-work-item-basic
lane: deterministic
target:
  kind: tool
  ref: control.capture_work_item
inputs:
  work_item:
    schema: runx.work_item.v1
    work_item_id: wi_fixture
    state: build_ready
    status_summary: Build is ready.
expect:
  status: success
  output:
    subset:
      present: true
      work_item:
        work_item_id: wi_fixture
        state: build_ready
