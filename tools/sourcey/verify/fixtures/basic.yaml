name: sourcey-verify-basic
lane: deterministic
target:
  kind: tool
  ref: sourcey.verify
workspace:
  files:
    site/index.html: |
      <!doctype html>
      <html><head><title>Fixture Docs</title></head><body><h1>Fixture Docs</h1></body></html>
inputs:
  output_dir: site
expect:
  status: success
  output:
    matches_packet: runx.sourcey.verification.v1
    subset:
      verified: true
      contains_doctype: true
