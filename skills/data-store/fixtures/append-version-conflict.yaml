name: append-version-conflict
kind: skill
target: ..
runner: append_event
inputs:
  data_source_ref: local://runx-data-store/version-conflict
  store_id: data-store-version-conflict-v1
  resource: generic_events
  aggregate_id: item-1
  expected_version: 1
  idempotency_key: item-1:create-conflict:v1
  event:
    type: item.created
    payload:
      label: conflict item
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
  steps:
    - append
metadata:
  public_skill: data-store
  source_case: append-version-conflict
  source: skills-fixture
  runner_kind: graph
  graph_shape: append_event_conflict
