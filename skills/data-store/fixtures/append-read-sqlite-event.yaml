name: append-read-sqlite-event
kind: skill
target: ..
runner: append_and_readback
env:
  RUNX_DATA_SOURCES: '{"data_sources":{"tenant://runx-data-store/sqlite-fixture":{"adapter":"data.sqlite","database_path":".runx/data/data-store-sqlite-fixture.sqlite","resources":{"board_events":{"kind":"event_stream","partition_key":"aggregate_id"}}}}}'
inputs:
  data_source_ref: tenant://runx-data-store/sqlite-fixture
  resource: board_events
  aggregate_id: sqlite-posting-123
  expected_version: 0
  idempotency_key: sqlite-posting-123:create:v1
  event:
    type: posting.created
    payload:
      title: verify sqlite-backed data-source resolution
      actor: runx:principal:example
      amount_minor: 500
      currency: usd
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
  steps:
    - append
    - readback
metadata:
  public_skill: data-store
  source_case: append-read-sqlite-event
  source: skills-fixture
  runner_kind: graph
  graph_shape: append_and_readback_sqlite
