name: append-local-event
kind: skill
target: ..
runner: append_event
inputs:
  data_source_ref: local://runx-data-store/append-fixture
  store_id: data-store-append-local-event-v1
  resource: generic_events
  aggregate_id: item-1
  expected_version: 0
  idempotency_key: item-1:create:v1
  event:
    type: item.created
    payload:
      label: first item
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
  steps:
    - append
metadata:
  public_skill: data-store
  source_case: append-local-event
  source: skills-fixture
  runner_kind: graph
  graph_shape: append_event
