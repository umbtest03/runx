name: evolve-plan-spec
kind: skill
target: ..
runner: evolve
inputs:
  objective: add release notes
  repo_root: .
  terminate: spec
env:
  RUNX_KNOWLEDGE_DIR: knowledge
caller:
  answers:
    agent_task.evolve-plan.output:
      objective_brief:
        objective: add release notes
        target_type: repo
        target_ref: .
      diagnosis_report:
        findings:
          - docs missing
        recommended_phases:
          - scope
          - model
      change_plan:
        steps:
          - draft release notes
        estimated_scope: small
        risk_assessment: low
      spec_document:
        spec_version: "1.1"
        task_id: evolve_release_notes
        phases:
          - scope
          - ingest
          - model
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
metadata:
  public_skill: evolve
  source_case: evolve-plan-spec
  source: skills-fixture
