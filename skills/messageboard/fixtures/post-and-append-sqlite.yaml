name: post-and-append-sqlite
kind: skill
target: ..
runner: post_and_append
env:
  RUNX_DATA_SOURCES: '{"data_sources":{"tenant://messageboard/sqlite-post":{"adapter":"data.sqlite","database_path":".runx/data/messageboard-post.sqlite","resources":{"board_events":{"kind":"event_stream","partition_key":"aggregate_id"}}}}}'
inputs:
  data_source_ref: tenant://messageboard/sqlite-post
  resource: board_events
  aggregate_id: post_sqlite_1
  expected_version: 0
  idempotency_key: post_sqlite_1:post:v1
  actor_kid: vendor
  title: Verify receipt link
  deliverable: Run the published verifier from a clean checkout and report the result.
  amount_minor: 5000
  currency: USD
  funding_evidence:
    hold_ref: mock:hold:post-sqlite-1
  clock_policy:
    claim_fuse_ms: 1800000
    delivery_deadline_ms: 86400000
    acceptance_window_ms: 43200000
caller:
  answers:
    agent_task.messageboard-post.output:
      actor_kid: vendor
      posting:
        id: post_sqlite_1
        title: Verify receipt link
        deliverable: Run the published verifier from a clean checkout and report the result.
        amount_minor: 5000
        currency: USD
        status: screening
      funding:
        funded_badge: true
        evidence_ref: mock:hold:post-sqlite-1
      clocks:
        claim_fuse_ms: 1800000
        delivery_deadline_ms: 86400000
        acceptance_window_ms: 43200000
      screening_notes:
        - Verify funding hold before approval.
      stop_conditions: []
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
  steps:
    - decide
    - persist
    - readback
metadata:
  public_skill: messageboard
  source_case: post-and-append-sqlite
  source: skills-fixture
  runner_kind: graph
  graph_shape: durable_post
