name: business-ops-smoke
kind: skill
target: ..
runner: main
inputs:
  signal: Launch readiness for API v2 with docs, release, customer comms, and spend checks.
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
  steps:
    - classify
    - docs
    - release
    - issue
    - send
    - spend
    - audit
metadata:
  public_skill: business-ops
  source_case: business-ops-smoke
  source: skills-fixture
  runner_kind: graph
  graph_shape: ops_fanout
