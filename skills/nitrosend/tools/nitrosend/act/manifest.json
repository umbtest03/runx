{
  "schema": "runx.tool.manifest.v1",
  "name": "nitrosend.act",
  "version": "0.2.0",
  "description": "Run one approval-gated Nitrosend MCP mutation and emit redacted provider evidence.",
  "source": {
    "type": "cli-tool",
    "command": "node",
    "args": ["./run.mjs"],
    "sandbox": {
      "profile": "network",
      "cwd_policy": "skill-directory",
      "network": true,
      "writable_paths": [],
      "require_enforcement": false,
      "env_allowlist": ["NITROSEND_API_KEY"]
    }
  },
  "inputs": {
    "operation": {
      "type": "string",
      "required": true,
      "description": "Bounded mutation: send_transactional, control_delivery, import_contacts, import_contacts_file, compose_campaign, compose_flow, manage_template, or define_segment."
    },
    "arguments": {
      "type": "json",
      "required": true,
      "description": "Arguments for the selected Nitrosend MCP tool."
    },
    "brand_sid": {
      "type": "string",
      "required": false,
      "description": "Optional public Nitrosend brand SID."
    }
  },
  "scopes": ["nitrosend:write"],
  "mutating": true,
  "retry": { "max_attempts": 1 },
  "output": {
    "packet": "runx.provider_evidence.v1",
    "wrap_as": "provider_evidence"
  }
}
