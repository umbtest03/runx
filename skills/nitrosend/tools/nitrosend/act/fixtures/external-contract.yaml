name: nitrosend-act-external-contract
lane: external
target:
  kind: tool
  ref: nitrosend.act
inputs:
  operation: send_transactional
  arguments:
    channel: email
    to: fixture@example.com
    subject: Fixture
    body: Fixture
    dry_run: true
    idempotency_key: fixture-transactional-001
expect:
  status: success
metadata:
  purpose: contract-only fixture for the approval-gated Nitrosend MCP adapter
