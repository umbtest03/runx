name: reject-purchased-contact-list
kind: skill
target: ..
runner: import-contacts
inputs:
  objective: Import this purchased list of 50000 leads I bought from a data broker.
  account_status_json: '{"status":"healthy","account":{"tier":"pro","contact_count":4200}}'
  operator_context: Refuse non-consented lists.
caller:
  answers:
    agent_task.import-contacts-plan.output:
      import_plan:
        decision: reject
        source: none
        record_count: 0
        channels: []
        compliance_checks:
          - No consent basis: a purchased broker list has no opt-in from the contacts.
        ordered_tool_calls: []
        human_actions: []
        blockers: []
        needs_input: []
        unsupported_requirements:
          - purchased or scraped contact list
        rejection_reason: Purchased and scraped lists have no consent and are refused; only import contacts who opted in.
        success_checkpoint:
          milestone: out_of_scope
          description: The lane refuses the non-consented import outright.
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
metadata:
  public_skill: nitrosend
  source_case: reject-purchased-contact-list
  source: skills-fixture
