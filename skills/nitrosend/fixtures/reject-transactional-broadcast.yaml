name: reject-transactional-broadcast
kind: skill
target: ..
runner: send-transactional
inputs:
  objective: Email our whole subscriber list about the spring sale.
  account_status_json: '{"status":"healthy","account":{"tier":"pro","can_send":true,"domain_verified":true},"config":{"from_email":"orders@acme.com"}}'
  operator_context: Reject anything that targets an audience rather than a single recipient.
caller:
  answers:
    agent_task.send-transactional-plan.output:
      transactional_plan:
        decision: reject
        channel: none
        recipient: ""
        idempotency_key: ""
        ordered_tool_calls: []
        human_actions: []
        blockers: []
        needs_input: []
        unsupported_requirements:
          - audience broadcast
        rejection_reason: A send to the whole list is a marketing broadcast; route to nitrosend/send-campaign, not a transactional send.
        success_checkpoint:
          milestone: out_of_scope
          description: The lane stops without sending to an audience and points the caller at send-campaign.
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
metadata:
  public_skill: nitrosend
  source_case: reject-transactional-broadcast
  source: skills-fixture
