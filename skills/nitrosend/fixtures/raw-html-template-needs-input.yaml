name: raw-html-template-needs-input
kind: skill
target: ..
runner: compose-email
inputs:
  objective: Just paste in my own raw HTML email and save it as a template.
  account_status_json: '{"status":"healthy","account":{"tier":"pro"},"brand":{"setup_complete":true,"company_name":"Acme Co"}}'
  operator_context: Sections only; redirect raw-HTML asks rather than inventing a raw-HTML format.
caller:
  answers:
    agent_task.compose-email-plan.output:
      email_design:
        decision: needs_input
        template_intent: none
        subject: ""
        preheader: ""
        sections: []
        rationale: Nitrosend has no raw-HTML template format; arbitrary HTML belongs inside a text section's content.
        ordered_tool_calls: []
        human_actions: []
        blockers: []
        needs_input:
          - Confirm the HTML can be placed inside a single text section, or provide the content so it can be structured into sections.
        unsupported_requirements:
          - raw-HTML template format
        success_checkpoint:
          milestone: awaiting_section_decision
          description: The skill stops for input rather than inventing an unsupported raw-HTML template.
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
metadata:
  public_skill: nitrosend
  source_case: raw-html-template-needs-input
  source: skills-fixture
