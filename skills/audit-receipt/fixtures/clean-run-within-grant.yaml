name: clean-run-within-grant
kind: skill
target: ..
runner: audit
inputs:
  receipt_id: rcpt_issue_triage_4471
  receipt_rows:
    - receipt_id: rcpt_issue_triage_4471
      skill_ref: issue-triage
      status: closed
      created_at: "2026-07-16T00:00:00Z"
      verification_status: verified
  receipt_summary: "Sealed run of issue-triage. Authority proof granted repo.read. Acts: one observation that read the issue thread. No mutating acts. Material referenced by hash (material_ref_hash present, no raw token). No denied requests."
  granted_scopes:
    - repo.read
  objective: Confirm the triage run stayed within its read-only grant.
caller:
  answers:
    agent_task.audit-receipt.output:
      receipt_audit:
        decision: ready
        run_ref: rcpt_issue_triage_4471
        granted_scopes:
          - repo.read
        exercised_scopes:
          - repo.read
        refusals: []
        findings:
          - id: receipt.clean
            severity: info
            message: Exercised authority (repo.read) is within the grant; no mutation to gate; material referenced by hash.
        verdict: clean
        rationale: The only exercised scope, repo.read, is covered by the proof. The single act is an observation, so there is no mutation requiring a gate, and material appears only as a hash reference. The run honored its bound.
        recommendations: []
        success_checkpoint:
          milestone: audit_clean
          description: A sealed read-only run audited as within its granted authority.
expect:
  status: sealed
  receipt:
    schema: runx.receipt.v1
    state: sealed
    disposition: closed
metadata:
  public_skill: audit-receipt
  source_case: clean-run-within-grant
  source: skills-fixture
