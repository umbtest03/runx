{
  "schema": "runx.tool.manifest.v1",
  "name": "demo.pet_get",
  "description": "Governed HTTP tool: GET a pet by id from a local fixture.",
  "source": {
    "type": "http",
    "url": "http://127.0.0.1:8732/v1/pets/{id}",
    "method": "GET",
    "allow_private_network": true
  },
  "inputs": {
    "id": {
      "type": "string",
      "required": true,
      "description": "Pet id; fills the {id} path placeholder."
    }
  },
  "scopes": ["demo.pet_get"]
}
