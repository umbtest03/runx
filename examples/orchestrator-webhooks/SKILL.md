---
name: orchestrator-webhooks
description: Outbound webhook templates for workflow orchestrators.
---

Use this example when a governed runx step should call an existing workflow
webhook after policy and credential handling stay inside runx.

The checked-in webhook URLs are placeholders. Replace them with operator-owned
webhook URLs before a live run.
