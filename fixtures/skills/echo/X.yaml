skill: echo
version: "0.1.0"

runners:
  default:
    default: true
    type: cli-tool
    command: sh
    args:
      - ./run.sh
    inputs:
      message:
        type: string
        required: true
        description: Message to echo.
