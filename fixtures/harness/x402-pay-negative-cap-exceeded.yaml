name: x402-pay-negative-cap-exceeded
kind: graph
target: ../graphs/payment/x402-pay-negative-cap-exceeded.yaml
caller:
  approvals:
    spend-approval: true
expect:
  status: failure
  steps:
    - quote
    - reserve
    - approve-spend
