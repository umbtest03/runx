# Bug Project Fixture

Minimal fixture workspace for the runx scafld bug-to-PR graph.

The graph receives this path through the explicit `fixture` graph input and uses
it as the scafld working directory.
