{
  "name": "runx",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.18.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build-workspace.mjs --pack",
    "build:pack": "node scripts/build-workspace.mjs --pack",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "test": "node scripts/test-workspace.mjs",
    "test:fast": "vitest run --config vitest.fast.config.ts",
    "lint": "pnpm typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
